# Clippy Configuration for Security Lints
# https://doc.rust-lang.org/clippy/

# Warn on all clippy lints
warn = [
    "clippy::all",
    "clippy::unwrap_used",
    "clippy::expect_used",
    "clippy::panic",
    "clippy::unimplemented",
    "clippy::todo",
    "clippy::unreachable",
    "clippy::mem_forget",
    "clippy::cast_ptr_alignment",
    "clippy::transmute_ptr_to_ptr",
]

# Allow certain lints if needed
allow = []
